---
title: "{{ title }}"
meta_desc: "{{ description }}"
metadata:
  id: {{ id }}
  title: "{{ title }}"
  description: "{{ description }}"
  url: {{ url }}
  runtime: {{ runtime }}
  lastUpdate: {{ lastUpdate }}
  duration: {{ duration }}
  {{ resources }}
summary: "{{ summary }}"
---

{{{ readme }}}
